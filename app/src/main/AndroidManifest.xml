<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Permissions -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.CHANGE_WIFI_STATE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <application
        android:name=".BabySleepMonitorApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.BabySleepMonitor"
        android:usesCleartextTraffic="true"
        tools:targetApi="31">
        
        <!-- Welcome/Onboarding Activity (Launcher) -->
        <activity
            android:name=".WelcomeActivity"
            android:exported="true"
            android:theme="@style/Theme.BabySleepMonitor">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        
        <!-- Server Setup Activity -->
        <activity
            android:name=".ServerSetupActivity"
            android:exported="false"
            android:theme="@style/Theme.BabySleepMonitor" />
        
        <!-- Connection Error Activity -->
        <activity
            android:name=".ConnectionErrorActivity"
            android:exported="false"
            android:theme="@style/Theme.BabySleepMonitor" />
        
        <!-- Main Monitor Activity -->
        <activity
            android:name=".MonitorActivity"
            android:exported="false"
            android:theme="@style/Theme.BabySleepMonitor" />
        
        <!-- Detection Zone Activity -->
        <activity
            android:name=".DetectionZoneActivity"
            android:exported="false"
            android:theme="@style/Theme.BabySleepMonitor" />
        
        <!-- Legacy Main Activity (kept for compatibility) -->
        <activity
            android:name=".MainActivity"
            android:exported="false"
            android:theme="@style/Theme.BabySleepMonitor" />
        
        <!-- Settings Activity -->
        <activity
            android:name=".SettingsActivity"
            android:exported="false"
            android:theme="@style/Theme.BabySleepMonitor" />
        
        <!-- Sleep History Activity -->
        <activity
            android:name=".SleepHistoryActivity"
            android:exported="false"
            android:theme="@style/Theme.BabySleepMonitor" />
        
        <!-- Sleep Report (Detail) Activity -->
        <activity
            android:name=".SleepReportActivity"
            android:exported="false"
            android:theme="@style/Theme.BabySleepMonitor" />

        <!-- Background Monitoring Service -->
        <service
            android:name=".MonitoringService"
            android:enabled="true"
            android:exported="false"
            android:foregroundServiceType="dataSync" />
            
        <!-- Boot Receiver to restart service after device reboot -->
        <receiver
            android:name=".BootReceiver"
            android:enabled="true"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>
        
    </application>

</manifest>
